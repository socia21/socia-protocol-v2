{
  "name": "socia-frontend",
  "version": "1.0.0",
  "description": "SOCIA Protocol Frontend",
  "main": "index.html",
  "scripts": {
    "start": "npx serve -s . -l $PORT"
  },
  dependencies": {
    "serve": "^14.2.4"
  }
}